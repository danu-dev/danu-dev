# ponytail: disabled unused Laravel CI in profile repo to keep Actions green
name: Disabled
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow disabled"

